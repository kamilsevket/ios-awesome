import SwiftUI

/// Defines the visual style variants for DSTextField.
public enum DSTextFieldStyle: CaseIterable {
    /// Standard underline style with a bottom border.
    case standard

    /// Outlined style with a full border around the field.
    case outlined

    /// Search style optimized for search inputs with a filled background.
    case search
}
